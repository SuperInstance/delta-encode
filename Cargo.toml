[package]
name = "delta-encode"
version = "0.1.0"
edition = "2021"
description = "Delta encoding and prediction: fixed delta, variable-length delta, XOR delta, zigzag encoding, and prediction-based encoding"
license = "MIT"
repository = "https://github.com/SuperInstance/delta-encode"
keywords = ["delta", "encoding", "zigzag", "xor", "compression"]
categories = ["algorithms", "compression", "encoding"]
readme = "README.md"

[lib]
name = "delta_encode"
path = "src/lib.rs"
